<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nowcoder.community.dao.RecordMapper">

    <sql id="insertFields">
        user_id,entity_type,entity_id,opera_type,create_time
    </sql>

    <sql id="selectFields">
        id,user_id,entity_type,entity_id,opera_type,create_time
    </sql>
    <insert id="insertRecord">
        insert into record <include refid="insertFields"></include>
        values (#{user_id},#{entity_type},#{entity_id},#{opera_type},#{create_time})
    </insert>
    <select id="selectRecord" resultType="Record">
        select <include refid="selectFields"></include>
        from record
        where user_id = #{userId}
        and entity_type = #{entityType}
        and entity_id = #{entityId}
        and opera_type = #{operaType}
    </select>


</mapper>